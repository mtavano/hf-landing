# Reproducible CI/Docker builds: disable pnpm's minimum-release-age guard.
# It rejects freshly published transitive deps and is meant for interactive
# installs, not deploy builds off a pinned lockfile.
minimum-release-age=0
